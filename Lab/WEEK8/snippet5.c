/*

Write a C program using functions without pointers to merge two sorted arrays into a single sorted array.
The program should take the sizes and elements of two sorted arrays as input.
Use functions to read, merge, and display arrays.
Ensure the output is a single merged, sorted array without any additional sorting.

*/

#include <stdio.h>

